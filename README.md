# Simple Shell

A minimal Unix-style shell written in C that demonstrates how Unix systems
locate and execute programs.

## Features
- Built-in commands: `cd`, `pwd`, `exit`
- PATH searching for executables
- Support for absolute and relative paths
- Argument parsing
- Process creation using `fork` and `execve`
